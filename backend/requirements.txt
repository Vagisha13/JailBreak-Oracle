fastapi==0.110.0
uvicorn==0.28.0
pydantic==2.6.4
pydantic-settings==2.2.1
sqlalchemy==2.0.28
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
pgvector==0.2.5
greenlet==3.0.3
# litellm floor: 1.53.9 is the first with the httpx<0.28 constraint AND the
# InternalServerError exception used by app.targets.litellm.
# litellm ceiling: 1.56.10+ (incl. 1.57.0 and later) lifts the httpx<0.28
# constraint, which conflicts with the pinned fastapi==0.110.0 / httpx==0.27.0
# stack (starlette TestClient is incompatible with httpx>=0.28).
litellm>=1.53.9,<1.56.10
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.7
aiosqlite>=0.20.0
# Firebase Admin SDK — server-side Cloud Firestore for distributed rate limiting
# (postgreSQL remains the system of record; Firestore is not used for app data).
# Capped <6.9.0 because 6.9+/7.x pin httpx[http2]==0.28.1, which conflicts with
# the pinned httpx==0.27.0 stack required by litellm and starlette's TestClient.
# Firestore only needs google-api-core[grpc]/google-cloud-firestore (gRPC), so
# the SDK's httpx REST path is never used.
firebase-admin>=6.5.0,<6.9.0
pytest==8.1.1
pytest-asyncio==0.23.5
httpx==0.27.0
mypy==1.9.0
flake8==7.0.0
